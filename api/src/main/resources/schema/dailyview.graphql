scalar DateTime
scalar Upload

type Mutation {
    createMember(input: CreateMemberInput!): Boolean
    login(input: LoginInput!): AuthToken
    reissue: AuthToken
    uploadImage(input: Upload!): Boolean
}